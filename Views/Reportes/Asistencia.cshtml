@model IEnumerable<dynamic>
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Reporte de Asistencia";
    ViewData["Subtitle"] = "Control de check-ins - PowerZone";

    int totalCheckins = 0;
    foreach (var item in Model) { totalCheckins += Convert.ToInt32(item.Total); }
    double promedioDiario = Model.Any() ? (double)totalCheckins / Model.Count() : 0;
}

<style>
    :root {
        --pz-blue: #2B4FD8;
        --pz-blue-dark: #1a3ab5;
        --pz-gold: #D4A017;
        --pz-gold-dark: #b8880e;
        --pz-dark: #0f1535;
        --pz-dark2: #1a2050;
        --pz-card: #111827;
        --pz-border: #1f2a4a;
        --pz-text: #e5e5e5;
        --pz-muted: #6b7280;
    }

    .pz-page {
        background: var(--pz-dark);
        min-height: 100vh;
        padding: 1.5rem;
    }

    .pz-card {
        background: var(--pz-card);
        border: 1px solid var(--pz-border);
        border-radius: 1rem;
    }

    .pz-label {
        display: block;
        font-size: .78rem;
        font-weight: 800;
        color: var(--pz-muted);
        text-transform: uppercase;
        letter-spacing: .08em;
        margin-bottom: .5rem;
    }

    .pz-input {
        background: #1a2050;
        border: 2px solid var(--pz-border);
        color: var(--pz-text);
        border-radius: .6rem;
        padding: .75rem 1rem;
        outline: none;
        font-weight: 600;
        transition: border-color .2s;
        width: 100%;
    }

        .pz-input:focus {
            border-color: #D4A017;
            box-shadow: 0 0 0 3px rgba(212,160,23,0.15);
        }

    .pz-stat {
        background: var(--pz-card);
        border-radius: .85rem;
        padding: 1.5rem;
        border-left: 4px solid;
    }

    .pz-btn-filter {
        background: linear-gradient(135deg, #2B4FD8, #1a3ab5);
        color: #fff;
        font-weight: 900;
        padding: .8rem 2rem;
        border-radius: .6rem;
        border: none;
        cursor: pointer;
        text-transform: uppercase;
        letter-spacing: .06em;
        display: inline-flex;
        align-items: center;
        gap: .5rem;
        transition: all .2s;
        box-shadow: 0 4px 14px rgba(43,79,216,0.3);
    }

        .pz-btn-filter:hover {
            background: linear-gradient(135deg, #D4A017, #b8880e);
            box-shadow: 0 6px 20px rgba(212,160,23,0.4);
        }

    .pz-table {
        width: 100%;
        border-collapse: collapse;
    }

        .pz-table th {
            background: linear-gradient(135deg, #0f1535, #1a2050);
            color: #D4A017;
            font-size: .75rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: .08em;
            padding: .9rem 1.5rem;
            text-align: left;
            border-bottom: 2px solid #D4A017;
        }

        .pz-table td {
            padding: .85rem 1.5rem;
            border-bottom: 1px solid var(--pz-border);
        }

        .pz-table tr:hover td {
            background: rgba(43,79,216,0.06);
        }

        .pz-table tr:last-child td {
            border-bottom: none;
        }

    .pz-badge {
        display: inline-flex;
        align-items: center;
        gap: .3rem;
        padding: .3rem .8rem;
        border-radius: 9999px;
        font-size: .72rem;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: .06em;
    }

    .pz-btn-sm {
        display: inline-flex;
        align-items: center;
        gap: .35rem;
        padding: .4rem .9rem;
        border-radius: .5rem;
        font-weight: 700;
        font-size: .75rem;
        border: none;
        cursor: pointer;
        text-transform: uppercase;
        letter-spacing: .05em;
        transition: all .2s;
    }

        .pz-btn-sm:hover {
            opacity: .85;
            transform: translateY(-1px);
        }

    .pz-modal-backdrop {
        background: rgba(0,0,0,.75);
        backdrop-filter: blur(4px);
    }

    .pz-modal {
        background: var(--pz-card);
        border: 1px solid var(--pz-border);
        border-radius: 1rem;
        overflow: hidden;
    }

    @@keyframes spin {
        to {
            transform: rotate(360deg);
        }
    }
</style>

<div class="pz-page space-y-6">

    <!-- Header Brand -->
    <div style="display:flex;align-items:center;gap:.75rem;">
        <div style="background:linear-gradient(135deg,#0f1535,#1a2050);padding:.4rem .9rem;border-radius:.5rem;border-bottom:2px solid #D4A017;box-shadow:0 4px 14px rgba(43,79,216,0.3);">
            <span style="font-size:1rem;font-weight:900;color:#D4A017;letter-spacing:.05em;">POWER</span><span style="font-size:1rem;font-weight:900;color:#fff;opacity:0.85;">ZONE</span>
        </div>
        <div>
            <h2 style="font-size:1.5rem;font-weight:900;color:#fff;text-transform:uppercase;letter-spacing:.04em;">Reporte de Asistencia</h2>
            <p style="color:var(--pz-muted);font-size:.82rem;">Control de check-ins del gimnasio</p>
        </div>
    </div>

    <!-- Filtro de fechas -->
    <div class="pz-card p-6">
        <div style="display:flex;flex-wrap:wrap;align-items:flex-end;gap:1rem;">
            <div style="flex:1;min-width:200px;">
                <label class="pz-label">Fecha Desde</label>
                <input type="date" id="fecha-desde" value="@ViewBag.FechaInicio.ToString("yyyy-MM-dd")" class="pz-input">
            </div>
            <div style="flex:1;min-width:200px;">
                <label class="pz-label">Fecha Hasta</label>
                <input type="date" id="fecha-hasta" value="@ViewBag.FechaFin.ToString("yyyy-MM-dd")" class="pz-input">
            </div>
            <button onclick="filtrarPorFecha()" class="pz-btn-filter">
                <i data-lucide="filter" style="width:1rem;height:1rem;"></i>
                Filtrar
            </button>
        </div>
    </div>

    <!-- Stats -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
        <div class="pz-stat" style="border-left-color:#2B4FD8;">
            <div style="display:flex;align-items:center;justify-content:space-between;">
                <div>
                    <p style="font-size:.75rem;font-weight:800;color:var(--pz-muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.35rem;">Total Check-ins</p>
                    <p style="font-size:2.25rem;font-weight:900;color:#fff;">@totalCheckins</p>
                </div>
                <div style="width:3.5rem;height:3.5rem;background:rgba(43,79,216,0.15);border-radius:50%;display:flex;align-items:center;justify-content:center;">
                    <i data-lucide="users" style="width:1.6rem;height:1.6rem;color:#2B4FD8;"></i>
                </div>
            </div>
        </div>

        <div class="pz-stat" style="border-left-color:#D4A017;">
            <div style="display:flex;align-items:center;justify-content:space-between;">
                <div>
                    <p style="font-size:.75rem;font-weight:800;color:var(--pz-muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.35rem;">Promedio Diario</p>
                    <p style="font-size:2.25rem;font-weight:900;color:#D4A017;">@Math.Round(promedioDiario, 1)</p>
                </div>
                <div style="width:3.5rem;height:3.5rem;background:rgba(212,160,23,0.15);border-radius:50%;display:flex;align-items:center;justify-content:center;">
                    <i data-lucide="trending-up" style="width:1.6rem;height:1.6rem;color:#D4A017;"></i>
                </div>
            </div>
        </div>

        <div class="pz-stat" style="border-left-color:#a78bfa;">
            <div style="display:flex;align-items:center;justify-content:space-between;">
                <div>
                    <p style="font-size:.75rem;font-weight:800;color:var(--pz-muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.35rem;">Día Más Activo</p>
                    <p style="font-size:2.25rem;font-weight:900;color:#a78bfa;">@(Model.Any() ? Model.OrderByDescending(x => x.Total).First().Total : 0)</p>
                </div>
                <div style="width:3.5rem;height:3.5rem;background:#7c3aed22;border-radius:50%;display:flex;align-items:center;justify-content:center;">
                    <i data-lucide="award" style="width:1.6rem;height:1.6rem;color:#a78bfa;"></i>
                </div>
            </div>
        </div>

        <div class="pz-stat" style="border-left-color:#f59e0b;">
            <div style="display:flex;align-items:center;justify-content:space-between;">
                <div>
                    <p style="font-size:.75rem;font-weight:800;color:var(--pz-muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.35rem;">Días Analizados</p>
                    <p style="font-size:2.25rem;font-weight:900;color:#fbbf24;">@Model.Count()</p>
                </div>
                <div style="width:3.5rem;height:3.5rem;background:#b4530922;border-radius:50%;display:flex;align-items:center;justify-content:center;">
                    <i data-lucide="calendar" style="width:1.6rem;height:1.6rem;color:#fbbf24;"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Gráfica -->
    <div class="pz-card p-8">
        <h3 style="font-size:1.25rem;font-weight:900;color:#fff;text-transform:uppercase;letter-spacing:.06em;margin-bottom:1.5rem;display:flex;align-items:center;gap:.5rem;">
            <span style="display:block;width:3px;height:1.1rem;background:#D4A017;border-radius:2px;"></span>
            Tendencia de Asistencia
        </h3>
        <div style="position:relative;height:350px;">
            <canvas id="chart-asistencia"></canvas>
        </div>
    </div>

    <!-- Tabla Detalle -->
    <div class="pz-card" style="overflow:hidden;">
        <div style="padding:1rem 1.5rem;border-bottom:2px solid #D4A017;background:linear-gradient(135deg,#0f1535,#1a2050);display:flex;align-items:center;gap:.5rem;">
            <span style="display:block;width:3px;height:1rem;background:#D4A017;border-radius:2px;"></span>
            <h3 style="font-size:1rem;font-weight:900;color:#fff;text-transform:uppercase;letter-spacing:.06em;">Detalle por Fecha</h3>
        </div>
        <div style="overflow-x:auto;">
            <table class="pz-table">
                <thead>
                    <tr>
                        <th>Fecha</th>
                        <th>Día</th>
                        <th style="text-align:center;">Check-ins</th>
                        <th style="text-align:center;">Estado</th>
                        <th style="text-align:center;">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.OrderByDescending(x => x.Fecha))
                    {
                        var fecha = (DateTime)item.Fecha;
                        var total = (int)item.Total;
                        var fechaStr = fecha.ToString("yyyy-MM-dd");
                        <tr>
                            <td><span style="font-weight:800;color:#fff;">@fecha.ToString("dd/MM/yyyy")</span></td>
                            <td><span style="color:var(--pz-muted);font-weight:600;">@fecha.ToString("dddd", new System.Globalization.CultureInfo("es-ES"))</span></td>
                            <td style="text-align:center;">
                                <span class="pz-badge" style="background:rgba(43,79,216,0.15);color:#7094f0;border:1px solid rgba(43,79,216,0.3);">
                                    @total visitas
                                </span>
                            </td>
                            <td style="text-align:center;">
                                @if (total >= 30)
                                {
                                    <span class="pz-badge" style="background:#05966922;color:#34d399;border:1px solid #05966944;">
                                        <i data-lucide="trending-up" style="width:.75rem;height:.75rem;"></i> Alto
                                    </span>
                                }
                                else if (total >= 15)
                                {
                                    <span class="pz-badge" style="background:rgba(212,160,23,0.12);color:#D4A017;border:1px solid rgba(212,160,23,0.3);">
                                        <i data-lucide="minus" style="width:.75rem;height:.75rem;"></i> Medio
                                    </span>
                                }
                                else
                                {
                                    <span class="pz-badge" style="background:#dc262622;color:#f87171;border:1px solid #dc262644;">
                                        <i data-lucide="trending-down" style="width:.75rem;height:.75rem;"></i> Bajo
                                    </span>
                                }
                            </td>
                            <td style="text-align:center;">
                                <button onclick="verAsistentes('@fechaStr', '@total')"
                                        class="pz-btn-sm"
                                        style="background:linear-gradient(135deg,#2B4FD8,#1a3ab5);color:#fff;"
                                        onmouseover="this.style.background='linear-gradient(135deg,#D4A017,#b8880e)';"
                                        onmouseout="this.style.background='linear-gradient(135deg,#2B4FD8,#1a3ab5)';">
                                    <i data-lucide="users" style="width:.9rem;height:.9rem;"></i>
                                    Ver Asistentes
                                </button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- MODAL ASISTENTES -->
<div id="modal-asistentes" class="hidden fixed inset-0 pz-modal-backdrop flex items-center justify-center z-50 px-4">
    <div class="pz-modal max-w-4xl w-full max-h-[90vh] overflow-hidden" style="box-shadow:0 25px 60px rgba(0,0,0,.8);border-color:#D4A017;">

        <!-- Header Modal -->
        <div style="padding:1.25rem 1.5rem;border-bottom:2px solid #D4A017;background:linear-gradient(135deg,#0f1535,#1a2050);display:flex;align-items:center;justify-content:space-between;">
            <div>
                <div style="display:flex;align-items:center;gap:.6rem;margin-bottom:.2rem;">
                    <span style="background:linear-gradient(135deg,#0f1535,#1a2050);color:#D4A017;font-size:.65rem;font-weight:900;padding:.15rem .55rem;border-radius:9999px;letter-spacing:.1em;border:1px solid #D4A017;">POWERZONE</span>
                    <h3 style="font-size:1.25rem;font-weight:900;color:#fff;text-transform:uppercase;letter-spacing:.05em;">Asistentes del Día</h3>
                </div>
                <p style="font-size:.82rem;color:var(--pz-muted);font-weight:600;" id="modal-fecha"></p>
            </div>
            <button onclick="cerrarModal()"
                    style="color:var(--pz-muted);background:#1f2a4a;border:1px solid var(--pz-border);border-radius:.5rem;padding:.4rem;cursor:pointer;transition:color .2s;"
                    onmouseover="this.style.color='#D4A017'; this.style.borderColor='#D4A017';"
                    onmouseout="this.style.color='var(--pz-muted)'; this.style.borderColor='var(--pz-border)';">
                <i data-lucide="x" style="width:1.25rem;height:1.25rem;display:block;"></i>
            </button>
        </div>

        <!-- Loading -->
        <div id="modal-loading" class="hidden" style="padding:3rem;text-align:center;">
            <div style="display:inline-block;width:3rem;height:3rem;border:3px solid var(--pz-border);border-top-color:#D4A017;border-radius:50%;animation:spin 1s linear infinite;"></div>
            <p style="margin-top:1rem;color:var(--pz-muted);font-weight:600;">Cargando asistentes...</p>
        </div>

        <!-- Content -->
        <div id="modal-content" style="padding:1.5rem;overflow-y:auto;max-height:calc(90vh - 180px);">
            <div id="asistentes-lista"></div>
        </div>

        <!-- Footer Modal -->
        <div style="padding:1rem 1.5rem;border-top:1px solid var(--pz-border);background:#0f1535;display:flex;align-items:center;justify-content:space-between;">
            <p style="font-size:.875rem;font-weight:700;color:var(--pz-muted);">
                Total de asistentes: <span id="modal-total" style="color:#D4A017;font-size:1.1rem;font-weight:900;">0</span>
            </p>
            <button onclick="cerrarModal()"
                    style="padding:.5rem 1.25rem;background:#1f2a4a;color:var(--pz-text);font-weight:700;border-radius:.5rem;border:1px solid var(--pz-border);cursor:pointer;text-transform:uppercase;font-size:.8rem;transition:all .2s;"
                    onmouseover="this.style.background='#263060'; this.style.borderColor='#D4A017';"
                    onmouseout="this.style.background='#1f2a4a'; this.style.borderColor='var(--pz-border)';">
                Cerrar
            </button>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        const asistenciaData = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model));

        function filtrarPorFecha() {
            const desde = document.getElementById('fecha-desde').value;
            const hasta = document.getElementById('fecha-hasta').value;
            window.location.href = `/reportes/asistencia?desde=${desde}&hasta=${hasta}`;
        }

        function renderChart() {
            const ctx = document.getElementById('chart-asistencia');
            const labels = asistenciaData.map(d => {
                const fecha = new Date(d.Fecha);
                return fecha.toLocaleDateString('es-ES', { day: '2-digit', month: 'short' });
            });
            const data = asistenciaData.map(d => d.Total);

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [{
                        label: 'Check-ins',
                        data: data,
                        borderColor: '#D4A017',
                        backgroundColor: 'rgba(212,160,23,0.08)',
                        borderWidth: 3,
                        fill: true,
                        tension: 0.4,
                        pointRadius: 4,
                        pointHoverRadius: 7,
                        pointBackgroundColor: '#D4A017',
                        pointBorderColor: '#111827',
                        pointBorderWidth: 2
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { display: false },
                        tooltip: {
                            backgroundColor: '#1a2050',
                            borderColor: '#1f2a4a',
                            borderWidth: 1,
                            titleColor: '#e5e5e5',
                            bodyColor: '#D4A017',
                            padding: 12,
                            callbacks: { label: ctx => ctx.parsed.y + ' check-ins' }
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            grid: { color: 'rgba(43,79,216,0.08)' },
                            ticks: { color: '#6b7280', stepSize: 5 }
                        },
                        x: {
                            grid: { display: false },
                            ticks: { color: '#6b7280' }
                        }
                    }
                }
            });
        }

        async function verAsistentes(fecha, total) {
            const modal = document.getElementById('modal-asistentes');
            const modalFecha = document.getElementById('modal-fecha');
            const modalTotal = document.getElementById('modal-total');
            const modalLoading = document.getElementById('modal-loading');
            const modalContent = document.getElementById('modal-content');
            const asistentesLista = document.getElementById('asistentes-lista');

            modal.classList.remove('hidden');
            modalLoading.classList.remove('hidden');
            modalContent.classList.add('hidden');

            const fechaObj = new Date(fecha + 'T00:00:00');
            modalFecha.textContent = fechaObj.toLocaleDateString('es-ES', { weekday:'long', year:'numeric', month:'long', day:'numeric' });
            modalTotal.textContent = total;

            try {
                const response = await fetch(`/api/CheckIn/asistentes/${fecha}`);
                const data = await response.json();

                if (data.success && data.asistentes) {
                    if (data.asistentes.length === 0) {
                        asistentesLista.innerHTML = `
                            <div style="text-align:center;padding:3rem;">
                                <i data-lucide="users" style="width:4rem;height:4rem;margin:0 auto 1rem;color:#374151;display:block;"></i>
                                <p style="color:#6b7280;font-weight:600;">No hay asistentes registrados para este día</p>
                            </div>`;
                    } else {
                        let html = '<div class="grid grid-cols-1 md:grid-cols-2 gap-4">';
                        data.asistentes.forEach((asistente, index) => {
                            html += `
                                <div style="display:flex;align-items:center;gap:1rem;padding:1rem;background:#1a2050;border-radius:.75rem;border:1px solid #1f2a4a;transition:border-color .2s;"
                                     onmouseover="this.style.borderColor='#D4A017'"
                                     onmouseout="this.style.borderColor='#1f2a4a'">
                                    ${asistente.fotoBase64
                                        ? `<img src="data:image/jpeg;base64,${asistente.fotoBase64}" alt="${asistente.nombre}" style="width:3.5rem;height:3.5rem;border-radius:50%;object-fit:cover;border:2px solid #D4A017;" />`
                                        : `<div style="width:3.5rem;height:3.5rem;border-radius:50%;background:linear-gradient(135deg,#2B4FD8,#1a3ab5);display:flex;align-items:center;justify-content:center;font-size:1.25rem;font-weight:900;color:#fff;flex-shrink:0;">${asistente.nombre.charAt(0)}</div>`
                                    }
                                    <div style="flex:1;">
                                        <h4 style="font-weight:800;color:#fff;font-size:1rem;">${asistente.nombre}</h4>
                                        <div style="display:flex;align-items:center;gap:.5rem;margin-top:.25rem;">
                                            <span style="font-size:.78rem;color:#6b7280;font-weight:600;">
                                                <i data-lucide="clock" style="width:.75rem;height:.75rem;display:inline-block;margin-right:.2rem;"></i>${asistente.hora}
                                            </span>
                                            <span style="font-size:.7rem;padding:.15rem .5rem;background:rgba(43,79,216,0.15);color:#7094f0;border-radius:9999px;font-weight:700;text-transform:uppercase;">${asistente.metodo}</span>
                                        </div>
                                    </div>
                                    <div style="font-size:1.25rem;font-weight:900;color:#D4A017;">#${index + 1}</div>
                                </div>`;
                        });
                        html += '</div>';
                        asistentesLista.innerHTML = html;
                    }
                    setTimeout(() => { if (typeof lucide !== 'undefined') lucide.createIcons(); }, 100);
                } else {
                    asistentesLista.innerHTML = `<div style="text-align:center;padding:3rem;"><i data-lucide="alert-circle" style="width:4rem;height:4rem;margin:0 auto 1rem;color:#f87171;display:block;"></i><p style="color:#f87171;font-weight:600;">Error al cargar los asistentes</p></div>`;
                }
            } catch (error) {
                console.error(error);
                asistentesLista.innerHTML = `<div style="text-align:center;padding:3rem;"><i data-lucide="alert-circle" style="width:4rem;height:4rem;margin:0 auto 1rem;color:#f87171;display:block;"></i><p style="color:#f87171;font-weight:600;">Error al cargar los asistentes</p><p style="font-size:.82rem;color:#6b7280;margin-top:.5rem;">${error.message}</p></div>`;
            } finally {
                modalLoading.classList.add('hidden');
                modalContent.classList.remove('hidden');
            }
        }

        function cerrarModal() { document.getElementById('modal-asistentes').classList.add('hidden'); }
        document.getElementById('modal-asistentes').addEventListener('click', function(e) { if (e.target === this) cerrarModal(); });
        document.addEventListener('keydown', e => { if (e.key === 'Escape') cerrarModal(); });

        window.addEventListener('load', function() { renderChart(); lucide.createIcons(); });
    </script>
}