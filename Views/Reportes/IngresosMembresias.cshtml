@model List<VidaFit.Controllers.WEB.IngresoMembresiaReporte>
@using System.Text.Json
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Ingresos por Membresías";
    ViewData["Subtitle"] = "Análisis de pagos por tipo de plan - Tallest GYM";

    decimal totalIngresos = 0;
    int totalPagos = 0;

    if (Model != null && Model.Count > 0)
    {
        foreach (var item in Model)
        {
            totalIngresos += item.Total;
            totalPagos += item.CantidadPagos;
        }
    }
}

<style>
    :root {
        --tg-green: #2EAD4B;
        --tg-green-dark: #1e8a38;
        --tg-green-light: #f0fdf4;
        --tg-border: #c6e8cf;
        --tg-black: #1a1a1a;
        --tg-dark: #2D2D2D;
        --tg-white: #ffffff;
    }

    .tg-card {
        background: var(--tg-white);
        border-radius: 1.25rem;
        border: 2px solid #e5e7eb;
        box-shadow: 0 4px 20px rgba(0,0,0,0.06);
    }

    .tg-input {
        border: 2px solid #e5e7eb;
        border-radius: .65rem;
        padding: .75rem 1rem;
        font-weight: 600;
        transition: border-color .2s, box-shadow .2s;
        width: 100%;
        background: #fafafa;
    }

        .tg-input:focus {
            outline: none;
            border-color: var(--tg-green);
            box-shadow: 0 0 0 3px rgba(46,173,75,0.15);
        }

    .tg-btn-filter {
        background: linear-gradient(135deg, var(--tg-green), var(--tg-green-dark));
        color: var(--tg-white);
        font-weight: 900;
        padding: .75rem 2rem;
        border-radius: .75rem;
        border: none;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        gap: .5rem;
        transition: all .2s;
        text-transform: uppercase;
        letter-spacing: .07em;
        box-shadow: 0 4px 16px rgba(46,173,75,0.3);
    }

        .tg-btn-filter:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(46,173,75,0.4);
        }

    .tg-stat-card {
        background: var(--tg-white);
        border-radius: 1.25rem;
        box-shadow: 0 4px 16px rgba(0,0,0,0.06);
        padding: 1.5rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .tg-table-head {
        background: linear-gradient(135deg, var(--tg-black), var(--tg-dark));
    }

    .tg-plan-icon {
        width: 2.75rem;
        height: 2.75rem;
        border-radius: .75rem;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, var(--tg-green), var(--tg-green-dark));
        box-shadow: 0 3px 10px rgba(46,173,75,0.3);
    }

    .tg-progress-bar {
        height: .65rem;
        border-radius: 9999px;
        background: linear-gradient(135deg, var(--tg-green), var(--tg-green-dark));
    }
</style>

<div class="space-y-6">

    <!-- Filtros -->
    <div class="tg-card p-6">
        <div class="flex flex-wrap items-end gap-4">
            <div class="flex-1 min-w-[200px]">
                <label class="block text-xs font-black text-gray-600 mb-2 uppercase tracking-widest">Fecha Desde</label>
                <input type="date" id="fecha-desde" value="@ViewBag.FechaInicio.ToString("yyyy-MM-dd")" class="tg-input">
            </div>
            <div class="flex-1 min-w-[200px]">
                <label class="block text-xs font-black text-gray-600 mb-2 uppercase tracking-widest">Fecha Hasta</label>
                <input type="date" id="fecha-hasta" value="@ViewBag.FechaFin.ToString("yyyy-MM-dd")" class="tg-input">
            </div>
            <button onclick="filtrarPorFecha()" class="tg-btn-filter">
                <i data-lucide="filter" class="w-4 h-4"></i>
                Filtrar
            </button>
        </div>
    </div>

    @if (Model != null && Model.Count > 0)
    {
        <!-- Stats -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div class="tg-stat-card border-l-4" style="border-left-color:var(--tg-green);">
                <div>
                    <p class="text-xs font-black text-gray-500 mb-1 uppercase tracking-widest">Ingresos Totales</p>
                    <p class="text-4xl font-black text-gray-900">$@totalIngresos.ToString("N2")</p>
                </div>
                <div class="w-14 h-14 rounded-full flex items-center justify-center" style="background:var(--tg-green-light);">
                    <i data-lucide="dollar-sign" class="w-7 h-7" style="color:var(--tg-green);"></i>
                </div>
            </div>
            <div class="tg-stat-card border-l-4 border-blue-500">
                <div>
                    <p class="text-xs font-black text-gray-500 mb-1 uppercase tracking-widest">Total de Pagos</p>
                    <p class="text-4xl font-black text-gray-900">@totalPagos</p>
                </div>
                <div class="w-14 h-14 bg-blue-100 rounded-full flex items-center justify-center">
                    <i data-lucide="credit-card" class="w-7 h-7 text-blue-600"></i>
                </div>
            </div>
        </div>

        <!-- Gráfico -->
        <div class="tg-card p-8">
            <h3 class="text-xl font-black text-gray-900 mb-6 uppercase tracking-wide flex items-center gap-2">
                <span class="w-1 h-6 rounded-full inline-block" style="background:var(--tg-green);"></span>
                Ingresos por Tipo de Plan · Tallest GYM
            </h3>
            <div class="relative" style="height:400px;">
                <canvas id="chart-ingresos"></canvas>
            </div>
        </div>

        <!-- Tabla detalle -->
        <div class="tg-card overflow-hidden">
            <div class="px-6 py-4 tg-table-head">
                <h3 class="text-lg font-black text-white uppercase tracking-wide">Detalle por Plan</h3>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead class="bg-gray-50 border-b-2 border-gray-100">
                        <tr>
                            <th class="px-6 py-4 text-left text-xs font-black text-gray-500 uppercase tracking-widest">Plan</th>
                            <th class="px-6 py-4 text-center text-xs font-black text-gray-500 uppercase tracking-widest">Cantidad de Pagos</th>
                            <th class="px-6 py-4 text-right text-xs font-black text-gray-500 uppercase tracking-widest">Ingresos</th>
                            <th class="px-6 py-4 text-center text-xs font-black text-gray-500 uppercase tracking-widest">Participación</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y-2 divide-gray-100">
                        @foreach (var item in Model)
                        {
                            var porcentaje = totalIngresos > 0 ? (item.Total / totalIngresos * 100) : 0;
                            <tr class="hover:bg-gray-50 transition-colors">
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="flex items-center gap-3">
                                        <div class="tg-plan-icon">
                                            <i data-lucide="award" class="w-5 h-5 text-white"></i>
                                        </div>
                                        <span class="font-black text-gray-900">@item.PlanNombre</span>
                                    </div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-center">
                                    <span class="inline-flex items-center px-3 py-1.5 bg-blue-100 text-blue-800 font-black rounded-full text-xs uppercase tracking-wide">
                                        @item.CantidadPagos pagos
                                    </span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-right">
                                    <span class="text-2xl font-black" style="color:var(--tg-green);">$@item.Total.ToString("N2")</span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-center">
                                    <div class="flex items-center justify-center gap-2">
                                        <div class="w-24 bg-gray-200 rounded-full h-2.5">
                                            <div class="tg-progress-bar" style="width:@porcentaje.ToString("F1")%;"></div>
                                        </div>
                                        <span class="text-xs font-black text-gray-700">@porcentaje.ToString("F1")%</span>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
    else
    {
        <div class="tg-card p-12 text-center">
            <div class="w-24 h-24 rounded-full mx-auto mb-4 flex items-center justify-center" style="background:var(--tg-green-light);">
                <i data-lucide="inbox" class="w-12 h-12" style="color:var(--tg-green);"></i>
            </div>
            <h3 class="text-2xl font-black text-gray-900 mb-3 uppercase tracking-wide">No hay pagos en este período</h3>
            <p class="text-gray-500 font-semibold">Intenta seleccionar un rango de fechas diferente</p>
        </div>
    }
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        const ingresosData = @Html.Raw(JsonSerializer.Serialize(Model));

        function filtrarPorFecha() {
            const desde = document.getElementById('fecha-desde').value;
            const hasta = document.getElementById('fecha-hasta').value;
            window.location.href = `/reportes/ingresos-membresias?desde=${desde}&hasta=${hasta}`;
        }

        function renderChart() {
            if (!ingresosData || ingresosData.length === 0) return;
            const ctx = document.getElementById('chart-ingresos');
            const labels = ingresosData.map(i => i.PlanNombre);
            const totales = ingresosData.map(i => i.Total);

            new Chart(ctx, {
                type: 'bar',
                data: {
                    labels,
                    datasets: [{
                        label: 'Ingresos ($)',
                        data: totales,
                        backgroundColor: '#2EAD4B',
                        hoverBackgroundColor: '#1e8a38',
                        borderRadius: 8,
                        borderSkipped: false
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { display: false },
                        tooltip: {
                            backgroundColor: 'rgba(26,26,26,0.9)',
                            padding: 12,
                            callbacks: { label: ctx => '$' + ctx.parsed.y.toFixed(2) }
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            ticks: { callback: val => '$' + val },
                            grid: { color: 'rgba(0,0,0,0.04)' }
                        },
                        x: { grid: { display: false } }
                    }
                }
            });
        }

        window.addEventListener('load', function() {
            renderChart();
            lucide.createIcons();
        });
    </script>
}
